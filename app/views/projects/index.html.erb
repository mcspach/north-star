<div>
  <h1 class="">All Projects</h1>
  <p>[ Find me in app/views/projects/index.html.erb ]</p>
  <%# <button type="button" 
    class="btn btn-lg btn-danger" 
    data-bs-toggle="popover" 
    title="Popover title" 
    data-bs-content="Amazing content, right ?">
    Click to toggle popover
  </button> %>

  <div class="projects-listing">
        <div class="cards mt-0">
          <% @projects.each do |project| %>
          <%= link_to project_path(project) do %>
          <div class="card">
            <%# cl_image_tag(project.photos.first.key, crop: :fill, class: "card-image") %>
            <p><%=project.title%></p>
          </div>
          <% end %>
        <% end %>
      </div>
  </div>


  <%= link_to "Create New", new_project_path, class: "btn btn-primary" %>
</div>
