<div>
  <h1>Editing Task</h1>

  <turbo-frame id="<%= dom_id @project %>">
    <div id="<%= dom_id @project %>">
      <%= render "form", project: @project %>
      <%= link_to "Dismiss Changes", projects_path, class: "bg-gray-200 hover:bg-gray-300 text-gray-700 font-bold py-2 px-4 rounded inline-block" %>
    </div>
  </turbo-frame>
</div>
