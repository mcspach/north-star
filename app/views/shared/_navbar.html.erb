    <nav class="navbar navbar-nav navbar-expand-md bg-light" role="navigation">
            <a href="/" class="navbar-brand">
              <%= image_tag "um_logo_wordmark_black.svg", style: "min-width: 200px;", alt: "Ursa Major Logo" %>
            </a>
          <div class="d-flex">
            <% if user_signed_in? %>
              <span class="navbar-text">Signed in as <%= current_user.email %></span>
              <button class="btn btn-light">
                <%= link_to "Sign Out", destroy_user_session_path, data: { "turbo-method": :delete } %>
              </button>
            <% else %>
              <button class="btn btn-light">
                <%= link_to "Sign Up", new_user_registration_path, class: "" %>
              </button>
              <button class="btn btn-light">
                <%= link_to "Sign In", new_user_session_path, class: "" %>
              </button>
            <% end %>
          </div>
    </nav>