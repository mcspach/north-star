<div>
<h1><%= @project.title %></h1>

<p><%= @project.description %></p>

<ul>
  <% @project.phases.each do |phase| %>
    <li><%= link_to phase.title, phase %></li>
  <% end %>
</ul>

<%= link_to "Edit", edit_project_path(@project) %> |
<%= link_to "Back", projects_path %>
</div>
